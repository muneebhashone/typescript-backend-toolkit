{{#if AUTH}}
// This file will be replaced with the full can-access middleware
// when auth feature is enabled
export * from '../../../auth/middlewares/can-access';
{{else}}
// Auth is disabled - providing stub export
import { Request, Response, NextFunction } from 'express';
export const canAccess = () => (req: Request, res: Response, next: NextFunction) => next();
{{/if}}
